- alias: Markiser in baksida
  initial_state: 'on'
  trigger:
    platform: state
    entity_id: sun.sun
  condition:
    condition: and
    conditions:
    - condition: numeric_state
      entity_id: sun.sun
      value_template: '{{ state.attributes.elevation }}'
      below: 50  ## Sun should be a below this value
    - condition: numeric_state
      entity_id: sun.sun
      value_template: '{{ state.attributes.azimuth }}'
      above: 140  ## Based on values captured
    - condition: numeric_state
      entity_id: sensor.vader_wind_speed
      below: 1   ## Guesstimate
    - condition: numeric_state
      entity_id:  sensor.vader_precipitation
      below: 0.1  ## no rain
  action:
    #- service: cover.set_cover_position
    #  data:
    #    entity_id: cover.level
    #    position: 40
    - service: notify.pushbullet
      data:
        message: 'Markiser in baksida'
